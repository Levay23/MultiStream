kind = "web"
previewPath = "/"
title = "StreamSync Panel"
version = "1.0.0"
id = "artifacts/streamsync-panel"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/streamsync-panel run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/streamsync-panel", "run", "build" ]
publicDir = "artifacts/streamsync-panel/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
